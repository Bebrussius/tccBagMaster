<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="/sistema/public/css/styles.css">
    <title>Empresas</title>
    <style>
        body {
            background-color: #fff;
        }

        nav {
            background-color: #020060;
        }

        .custom-nav-link {
            color: #FFFFFF;
        }

        @media (max-width: 768px) {

            h2,
            h4 {
                font-size: 16px;
            }
        }

        @media (max-width: 768px) {
            img.img-fluid {
                max-width: 150px;
            }
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <a class="navbar-brand">
            <span class="text-white">BagMaster</span>
        </a>
        <img src="../../public/uploads/BagMaster.png" alt="">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#conteudoNavbarSuportado"
            aria-controls="conteudoNavbarSuportado" aria-expanded="false" aria-label="Alterna navegação">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="conteudoNavbarSuportado">
            <ul class="navbar-nav">
                {{#if user}}
                {{#if (eq user.funcao 'Administrador')}}

                <li class="nav-item">
                    <a class="nav-link" href="/empresaroutes">Clientes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/pedidoroutes">Pedidos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/usuarioroutes">Funcionários</a>
                </li>
                {{else}}
                {{#if (eq user.funcao 'Empresa')}}

                <li class="nav-item">
                    <a class="nav-link" href="/empresaroutes">Clientes</a>
                </li>
                {{#if (eq user.funcao 'Pedidos')}}

                <li class="nav-item">
                    <a class="nav-link" href="/pedidoroutes">Pedidos</a>
                </li>
                {{/if}}
                {{else}}

                <li class="nav-item">
                    <a class="nav-link" href="/pedidoroutes">Pedidos</a>
                </li>
                {{/if}}
                {{/if}}
                <li class="nav-item">
                    <a class="nav-link" href="/usuarioroutes/logout">Logout</a>
                </li>
                {{else}}
                <li class="nav-item">
                    <a class="nav-link" href="/login">Login</a>
                </li>
                {{/if}}
            </ul>
        </div>
    </nav>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="https://kit.fontawesome.com/5d50d9fba3.js" crossorigin="anonymous"></script>
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
</body>

</html>